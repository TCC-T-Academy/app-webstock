import { MovimentacaoDatasource } from './movimentacao-datasource';

describe('MovimentacaoDatasource', () => {
  it('should create an instance', () => {
    expect(new MovimentacaoDatasource()).toBeTruthy();
  });
});
